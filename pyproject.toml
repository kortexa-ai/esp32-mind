[project]
name = "esp32-mind"
version = "0.1.0"
description = "USB-prompted TinyStories language model for the XIAO ESP32-S3"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.1",
    "pyserial>=3.5",
    "requests>=2.34.2",
    "tokenizers>=0.23.1",
    "torch>=2.13.0",
    "tqdm>=4.69.0",
]
